function init()
map_size = [4, 4]; % [width, height]
map_matrix = zeros(map_size(1)*100, map_size(2)*100);
obs_dims = [0.01 0.01;
            0.11 0.012;
            0.03 0.02];
num_obs = 5;
obs_choices = [1, 2, 2, 3, 3];
for i=1:num_obs
   obs_dim = obs_dims(obs_choices(i), :);
   
   % Random position (upper_x, upper_y)
   upper_x = randi([5, 
end

end

